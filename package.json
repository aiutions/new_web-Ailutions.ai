{
  "name": "ailutions-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd frontend && yarn install && yarn build",
    "dev": "cd frontend && yarn start"
  },
  "workspaces": [
    "frontend"
  ]
}